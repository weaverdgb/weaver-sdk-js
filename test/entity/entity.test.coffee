$ = require("./../test-suite")()
cuid = require('cuid')
Entity = require('../../src/entity')

describe 'Entity: Fetching an entity', ->

  it 'should not fetch if already fetched', ->
    return

  it 'should fetch if not already fetched', ->
    return

  it 'should not load an entity if already available in the repository', ->
    return

  it 'should load an entity if not available in the repository', ->
    return

  it 'should fetch with default eagerness set to 1 if unspecified', ->
    return

  it 'should fetch endlessly if eagerness is set to -1', ->
    return

  it 'should correctly fetch with eagerness level 2', ->
    return

  it 'should correctly fetch with eagerness level 3', ->
    return

  it 'should correctly fetch with eagerness level 4', ->
    return

  it 'should correctly fetch a sub entity', ->
    return